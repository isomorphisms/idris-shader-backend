# Apple Watch graphics note

Watch hardware contains Apple GPUs/display engines across generations, but watchOS is not being treated here as a full-screen Metal research target equivalent to iOS/macOS. Keep CPU ABI coverage in the CPU atlas; only add shader deployment work when the public watchOS graphics API actually supports the required path.
