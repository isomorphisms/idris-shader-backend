# Historical Mac GPUs

Intel-era and earlier Macs shipped GPUs from Intel, AMD/ATI and NVIDIA in many generations. Do not create one fake `Mac GPU ISA`. Metal exposes Mac GPU-family/runtime feature queries; old OpenGL paths are separate legacy targets.
